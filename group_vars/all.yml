---
# ServiceNow Instance Configuration
# These values should be overridden with actual values
# either via extra-vars, environment variables, or vault

# Example values (override these with your actual values)
# snow_instance_url: "https://dev12345.service-now.com"
# snow_user: "admin"
# snow_pass: "{{ vault_snow_password }}"
# ci_number: "sys_id_of_your_ci"

# SSL Certificate validation
validate_ssl_certs: true